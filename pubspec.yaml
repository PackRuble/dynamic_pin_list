name: dynamic_pin_list
description: Dynamic pinning of items in a list using slivers
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.19.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  flutter_hooks: 0.18.6
  visibility_detector: 0.4.0+2
  sliver_tools: 0.2.9

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true
